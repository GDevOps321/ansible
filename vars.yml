- name: sample
  hosts: all
  vars:
    url: frooty.com
  tasks:
    - name: variables
      debug:
        msg: url = {{ url }}
    - name: local variable
      vars:
        SMP: avk.com
      debug:
        msg:
          SMP = {{ SMP }}